name: Aplicar pacote protetores gastricos

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  concluido:
    runs-on: ubuntu-latest
    steps:
      - name: Implantação já concluída
        run: echo 'O pacote permanente já foi aplicado e auditado na branch isolada.'
